[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn server.main:app --host 0.0.0.0 --port ${PORT:-8080} --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
